apiVersion: v1
kind: Service
metadata:
  name: orders-postgres
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: orders-postgres
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP